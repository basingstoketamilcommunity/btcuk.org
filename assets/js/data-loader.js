document.addEventListener('DOMContentLoaded',()=>{fetch('data/content.json').then(r=>r.json()).then(data=>{const people=document.getElementById('peoplePreview');if(people){data.trustees.slice(0,8).forEach(t=>people.insertAdjacentHTML('beforeend',`<article class="person"><img src="${t.photo}" alt="${t.name}"><h3>${t.name}</h3><p>${t.role}</p></article>`));}const grid=document.getElementById('eventsGrid');if(grid){data.events.forEach(e=>grid.insertAdjacentHTML('beforeend',`<article class="event-card"><img src="${e.poster}" alt="${e.title}"><div class="event-card-body"><span class="pill">Community event</span><h3>${e.title}</h3><p>${e.date}</p>${e.registration?`<a class="btn btn-primary" href="${e.registration}" target="_blank" rel="noopener">Register →</a>`:''}</div></article>`));}}).catch(err=>console.error('BTC content load error',err));});